# Defining an empty dictionary
dictionary = {}

dictionary['name'] = 'Diego' # Add a key with its value

dictionary['name'] = 90 # Re-assign the value of a key

value = dictionary['name']

print(value)

# Defining a dictionary with initial values
dictionary = {'a': 1, 'b': 2, 'c': 3, 'a': 4}

print(dictionary)